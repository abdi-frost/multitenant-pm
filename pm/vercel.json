{
  "ignoreCommand": "bash -lc 'git fetch origin \"$VERCEL_GIT_COMMIT_REF\" --depth=1 >/dev/null 2>&1 || git fetch origin main --depth=1 >/dev/null 2>&1 || true; git diff --name-only FETCH_HEAD..HEAD | grep -q \"^pm/\" || exit 0; exit 1'"
}
